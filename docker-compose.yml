version: "3.9"

services:
  sensor-network-manager:
    build: .
    container_name: sensor-network-manager
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./instance:/app/instance
      - ./app/uploads:/app/app/uploads
    restart: unless-stopped
